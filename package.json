{
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify && npx pagefind --site public",
    "preview": "npx serve public",
    "index": "npx pagefind --site public",
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:md": "markdownlint --ignore node_modules --ignore themes .",
    "lint": "npm run format:check && npm run lint:md"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "markdownlint-cli": "^0.48.0",
    "pagefind": "^1.3.0",
    "prettier": "^3.8.1",
    "prettier-plugin-go-template": "^0.0.15",
    "prettier-plugin-lint-md": "^1.0.1",
    "prettier-plugin-toml": "^2.0.6",
    "serve": "^14.2.4"
  },
  "lint-staged": {
    "*.{html,toml,js,scss,json,yaml,yml}": "prettier --write",
    "*.md": [
      "prettier --write",
      "markdownlint --fix"
    ]
  }
}
